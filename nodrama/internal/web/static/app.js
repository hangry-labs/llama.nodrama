"use strict";

/*
 * llama.nodrama UI is split into /static/app/*.js.
 * index.html loads those files in order; this stub remains only so stale direct
 * references to /static/app.js fail harmlessly instead of serving old code.
 */
